import { dynamicMetadata } from "@karrio/core/components/metadata";
export { default } from "@karrio/core/modules/Settings/profile";
export const generateMetadata = dynamicMetadata("Profile Settings");
