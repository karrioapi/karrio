import { dynamicMetadata } from "@karrio/core/components/metadata";
export { default } from "@karrio/core/modules/Workflows/events";
export const generateMetadata = dynamicMetadata("Workflow Events");