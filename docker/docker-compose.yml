services:
  api:
    container_name: jtl.api
    image: ghcr.io/jtlshipping/shipping-platform-server:${JTL_TAG}
    restart: unless-stopped
    ports:
      - 5002:5002
    depends_on:
      - db
      - redis
    environment:
      # Application Settings
      USE_HTTPS: "True"
      DEBUG_MODE: "False"
      DETACHED_WORKER: "True"
      SECRET_KEY: ${SECRET_KEY}
      ENABLE_ALL_PLUGINS_BY_DEFAULT: "True"

      # Database Configuration
      DATABASE_HOST: ${DATABASE_HOST:-db}
      DATABASE_NAME: ${DATABASE_NAME:-db}
      DATABASE_PORT: ${DATABASE_PORT:-5432}
      DATABASE_ENGINE: ${DATABASE_ENGINE:-postgresql_psycopg2}
      DATABASE_USERNAME: ${DATABASE_USERNAME:-postgres}
      DATABASE_PASSWORD: ${DATABASE_PASSWORD:-postgres}

      # Redis Configuration
      REDIS_HOST: ${REDIS_HOST:-redis}
      REDIS_PORT: ${REDIS_PORT:-6379}
    volumes:
      - jtl-static:/karrio/static
      - ${JTL_PLUGINS:-./plugins}:/karrio/plugins

  worker:
    container_name: jtl.worker
    image: ghcr.io/jtlshipping/shipping-platform-server:${JTL_TAG}
    restart: unless-stopped
    command: "/bin/bash ./worker"
    depends_on:
      - db
      - redis
      - api
    environment:
      # Application Settings
      DEBUG_MODE: "False"
      ENABLE_ALL_PLUGINS_BY_DEFAULT: "True"

      # Database Configuration
      DATABASE_HOST: ${DATABASE_HOST:-db}
      DATABASE_NAME: ${DATABASE_NAME:-db}
      DATABASE_PORT: ${DATABASE_PORT:-5432}
      DATABASE_ENGINE: ${DATABASE_ENGINE:-postgresql_psycopg2}
      DATABASE_USERNAME: ${DATABASE_USERNAME:-postgres}
      DATABASE_PASSWORD: ${DATABASE_PASSWORD:-postgres}

      # Redis Configuration
      REDIS_HOST: ${REDIS_HOST:-redis}
      REDIS_PORT: ${REDIS_PORT:-6379}
    volumes:
      - ${JTL_PLUGINS:-./plugins}:/karrio/plugins

  dashboard:
    container_name: jtl.dashboard
    image: ghcr.io/jtlshipping/shipping-platform-dashboard:${JTL_TAG}
    ports:
      - ${DASHBOARD_PORT:-3102}:${DASHBOARD_PORT:-3102}/tcp
    depends_on:
      - api
    environment:
      # Node.js Settings
      NODE_ENV: production
      DASHBOARD_PORT: ${DASHBOARD_PORT:-3102}

      # App URLs
      DASHBOARD_URL: ${DASHBOARD_URL}
      KARRIO_PUBLIC_URL: ${KARRIO_PUBLIC_URL}

      # Karrio API (Backend)
      VITE_KARRIO_API: ${KARRIO_PUBLIC_URL}

      # JTL Hub OAuth2 (Authorization Code Flow with PKCE) - For standalone access
      VITE_JTL_HUB_CLIENT_ID: ${JTL_HUB_CLIENT_ID}
      VITE_JTL_HUB_CLIENT_SECRET: ${JTL_HUB_CLIENT_SECRET:-}
      VITE_JTL_HUB_AUTHORIZE_URL: ${JTL_HUB_AUTHORIZE_URL:-https://auth.jtl-cloud.com/oauth2/auth}
      VITE_JTL_HUB_TOKEN_URL: ${JTL_HUB_TOKEN_URL:-https://auth.jtl-cloud.com/oauth2/token}
      VITE_JTL_HUB_REDIRECT_URI: ${DASHBOARD_URL}/auth/callback

  caddy:
    container_name: jtl.caddy
    image: caddy:2.6.2
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - jtl-static:/var/www/karrio/static
      - caddy-data:/data
    depends_on:
      - api
      - dashboard

  db:
    container_name: jtl.db
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${DATABASE_NAME:-db}
      POSTGRES_USER: ${DATABASE_USERNAME:-postgres}
      POSTGRES_PASSWORD: ${DATABASE_PASSWORD:-postgres}
      PGDATA: /var/lib/postgresql/data/jtl
    volumes:
      - postgres-data:/var/lib/postgresql/data

  redis:
    container_name: jtl.redis
    image: redis
    restart: unless-stopped
    volumes:
      - redis-data:/data

volumes:
  jtl-static:
  postgres-data:
  redis-data:
  caddy-data:
